const initialState = {
  items: [], //  用来存放搜索结果
  loading: false, //  加载状态
  error: false, //  错误信息
  index: 0, //  当前视图
  currPage: 1, //  当前页面
  scrollTop: 0 //  滚动条高度
};

export default (state = initialState, action) => {
  // 开始请求

  // 请求成功

  // 请求失败

  // 设置index

  // 设置currPage

  // 设置scrollTop
  return state;
};
